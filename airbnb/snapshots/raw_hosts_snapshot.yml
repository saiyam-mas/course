snapshots:
  - name: scd_raw_hosts
    relation: source('airbnb', 'hosts')
    config:
      unique_key: id
      updated_at: updated_at
      strategy: timestamp # Can be 'timestamp' or 'check' or a macro for custom strategy
      hard_deletes: invalidate